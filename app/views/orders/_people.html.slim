div data-role="panel" id="peoplePanel" data-position="right" data-display="overlay"  data-theme="b"

  form
    input data-type="search" id="filterPeople"

  div id="peopleButtons" data-role="controlgroup" data-filter="true" data-input="#filterPeople"
    - @people.each do |person|
      button href="#" class="ui-btn ui-shadow ui-corner-all"  #{person.name} (#{person.feature})
